<p style="color: green"><%= notice %></p>

<h1>Categories</h1>

<div id="writer_categories">
  <% @categories.each do |category| %>
    <%= render category %>
    <p>
      <%= link_to "Show this category", [:writer, category] %>
    </p>
  <% end %>
</div>

<%= link_to "New category", new_writer_category_path %>
