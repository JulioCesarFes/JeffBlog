<p style="color: green"><%= notice %></p>

<%= render @category %>

<div>
  <%= link_to "Edit this category", edit_writer_category_path(@category) %> |
  <%= link_to "Back to categories", writer_categories_path %>

  <%= button_to "Destroy this category", [:writer, @category], method: :delete, class: 'btn btn-danger' %>
</div>
